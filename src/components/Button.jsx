const Button = () => {
  return (
    <div>
      <button
        type="button"
        className={`py-1 px-2 bg-blue-gradient font-mont font-light text-2xl rounded-lg outline-none text-primary hover:opacity-80 `}
      >
        Hire Me
      </button>
    </div>
  );
};

export default Button;
